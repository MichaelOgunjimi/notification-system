Alert triggered: {{ rule_name }}

{{ metric_label }} in {{ project_name }} reached {{ observed_value }} over the last
{{ window_minutes }} minutes, past the {{ threshold_value }} threshold.

{{ action_url }}

This rule won't fire again until its evaluation window passes.
