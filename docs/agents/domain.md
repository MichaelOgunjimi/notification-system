# Domain docs

This repository uses a single domain context.

Before exploring or changing domain behavior, read:

- `CONTEXT.md` at the repository root
- Relevant decisions under `docs/adr/`, when present

Use the glossary's preferred terms in issues, implementation, tests, and documentation. If a proposal conflicts with an ADR, surface the conflict explicitly.
